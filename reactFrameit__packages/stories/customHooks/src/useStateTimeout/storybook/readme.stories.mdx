import { Meta, Story, Canvas } from '@storybook/addon-docs';

import TimeoutComponent from './index';

<Meta title="Hooks/useInputTmeout/readme" component={ TimeoutComponent } />

# TimeoutComponent

```javascript
import TimeoutComponent from '@codedevbrad/reactframeithooks';
```

```javascript
function TimeoutComponent( { wait = 1000 } ) {

    const func = ( value ) => {
        console.log(`update ${ value } after 1 second.`)
    }

    let [ value , setValue ] = useStateTimeout({
        initialValue: 'hey' , returnFunc: func , timeout: wait 
    });
 
    return (
        <div>
            <input type='text' value={value} onChange={ ( e ) => setValue(e.target.value) } />
        </div>
    );
}
```

```javascript
    <TimeoutComponent wait={ 1000 } />
```

<Canvas>
        <TimeoutComponent wait={ 3000 } />
</Canvas>
