

// detect if the user has reached a current element

// detect if the element is currently visible / and when it's not.

// intersection observer.

module.exports = ( ) => {
  console.log( 'working');
}
